# static/images 目录说明

图片按用途分类，详见仓库根目录 `AGENTS.md`。

- `site/` — 主题 UI（banner、icons、cursor）
- `covers/paccha/` — 文章封面
- `posts/vtuber/` — 共用 VTuber 配图
- `sponsor/` — 赞助相关

头像文件放在 `static/avatar/`（不在本目录下）。

文章正文图片请放在 `content/post/<slug>/` 与 `index.md` 同目录。
